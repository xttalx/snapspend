# SnapSpend uses Vite + Express (not Next.js). Same Supabase keys, different names.
# Copy to .env and fill in values from Supabase → Project Settings → API

# ─── Server (API on Vercel / local port 3456) ───
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret-key

# ─── Browser sign-in (use legacy anon JWT for Google + email auth) ───
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-public-key
SUPABASE_ANON_KEY=eyJ...same-anon-key-as-above

# ─── Auth ───
JWT_SECRET=generate-a-long-random-string
JWT_EXPIRES_IN=30d

# ─── App URLs ───
FRONTEND_URL=https://your-app.vercel.app
CORS_ORIGIN=https://your-app.vercel.app
# OAuth redirect (must be listed in Supabase Redirect URLs)
VITE_APP_URL=http://localhost:5173
VITE_API_URL=

# ─── AI (Snap chat) ───
ANTHROPIC_API_KEY=

MILE_RATE=0.67
PORT=3456
